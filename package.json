{
  "private": true,
  "name": "youtube-rss-finder",
  "version": "1.0.0",
  "description": "Helps you find the RSS feed of a YouTube channel.",
  "main": "background.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npx mocha test",
    "lint": "npx eslint --config .eslintrc.json",
    "lint:src": "npx eslint --config .eslintrc.json src/**/*.js",
    "lint:test": "npx eslint --config .eslintrc.json test/**/*.js",
    "lint:all": "npx eslint --config .eslintrc.json **/*.js",
    "fix": "npx eslint --config .eslintrc.json --fix",
    "fix:src": "npx eslint --config .eslintrc.json src/**/*.js --fix",
    "fix:test": "npx eslint --config .eslintrc.json test/**/*.js --fix",
    "fix:all": "npx eslint --config .eslintrc.json **/*.js --fix",
    "fix:dry": "npx eslint --config .eslintrc.json --fix-dry-run",
    "firefox": "npx web-ext run --source-dir ./src/ --firefox=firefox --firefox-profile=dev --start-url about:debugging",
    "firefox:dev": "npx web-ext run --source-dir ./src/ --firefox=firefoxdeveloperedition --firefox-profile=dev --start-url about:debugging",
    "firefox:nightly": "npx web-ext run --source-dir ./src/ --firefox=nightly --firefox-profile=nightly --start-url about:debugging",
    "build:firefox": "npx web-ext build --source-dir ./src/ --overwrite-dest --artifacts-dir ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teddy-gustiaux/youtube-rss-finder.git"
  },
  "author": "Teddy Gustiaux",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/teddy-gustiaux/youtube-rss-finder/issues"
  },
  "homepage": "https://github.com/teddy-gustiaux/youtube-rss-finder#readme",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^13.0.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-prettier": "^2.6.1",
    "mocha": "^5.2.0",
    "prettier": "^1.13.7",
    "web-ext": "^2.7.0"
  }
}
